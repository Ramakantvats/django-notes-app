version: '3.8'

services:
  web:
    image: django-image:latest
    container_name: django-app
    ports:
      - "8000:8000"
    environment:
      - DB_NAME=notesdb
      - DB_USER=djangouser
      - DB_PASSWORD=W4efrarfxP##
      - DB_HOST=172.17.0.1
      - DB_PORT=3306
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
